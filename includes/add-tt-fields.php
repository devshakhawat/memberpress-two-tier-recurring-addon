<?php // phpcs:ignore
namespace TTRECURRING;

// if direct access than exit the file.
defined( 'ABSPATH' ) || exit;

/**
 * Handles plugin shortcode.
 *
 * @since 1.0.0
 */
class Add_tt_fields {

   public function __construct() {
        // add_action( 'mepr-tt-fields', array( $this, 'add_tt_fields' ) );
        // pretty_log('Add_tt_fields class loaded');
    }


}